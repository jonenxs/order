spring:
  application:
    name: order
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: dev
  rabbitmq:
      addresses: 119.27.187.114:5672
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
